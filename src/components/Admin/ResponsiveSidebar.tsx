"use client";
import React from "react";
import Sidebar from "./Sidebar";

/**
 * ResponsiveSidebar component that handles both desktop and mobile layouts
 * This is the main component that should be imported and used in layouts
 */
const ResponsiveSidebar: React.FC = () => {
  return <Sidebar />;
};

export default ResponsiveSidebar;
